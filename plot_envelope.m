% Plots signal and envelope.
% Marko Mayr, 16.12.2017
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% 

    %subplot(2,1,1);
    %plot(dummy);
    xvec = xstart:xend;
    %xlim ([xstart xend]);
    
    % ydummy is the envelope function
    fitdummy = ydummy(xvec); 
    
    %%%%%%%%%%%%%%%
    %subplot(2,1,2);
    %plot(ydummy); 
    %xlim ([xstart xend]);
 
  
